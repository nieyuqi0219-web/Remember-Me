name: remember_me
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # --- UI 图标与字体 ---
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0

  # --- AI 大脑 ---
  google_generative_ai: ^0.4.7
  http: ^1.2.0  # 👈 加上这一行

  # 新增下面这两个
  gal: ^2.3.0
  permission_handler: ^11.3.1

  # --- 相机与相册 ---
  image_picker: ^1.1.2
  camera: ^0.11.0

  # --- 本地存储 ---
  path_provider: ^2.1.3
  sqflite: ^2.3.3
  path: ^1.9.0

  # --- 纯 Dart 图片处理 (替代 OpenCV) ---
  image: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # 你的资源图片
  assets:
    - assets/welcome_illustration.png